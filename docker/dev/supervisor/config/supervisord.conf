[unix_http_server]
file=/var/run/config.sock
chmod=0700

[inet_http_server]
port=0.0.0.0:9001
username=admin
password=ca@sca2ew!qh3e7-bhwqv

[supervisord]
user=root
nodaemon=true
logfile=/dev/null
pidfile=/tmp/supervisord.pid

[rpcinterface:supervisor]
supervisor.rpcinterface_factory = supervisor.rpcinterface:make_main_rpcinterface

[supervisorctl]
serverurl=unix:///var/run/config.sock ; use a unix:// URL  for a unix socket

[include]
files = /etc/supervisor/conf.d/*.conf
